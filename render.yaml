services:
  - type: web
    name: komik-reader-app
    env: node
    plan: free
    buildCommand: npm run build:prod
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.18.0
